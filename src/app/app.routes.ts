import { Routes } from '@angular/router';
import { HeroesPageComponent } from '@hero/containers/heroes-page/heroes-page.component';

export const routes: Routes = [
    {
        path: "",
        component: HeroesPageComponent,
    }
];
